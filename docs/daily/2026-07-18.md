# 2026-07-18

**Project:** NeNe Invoice（自己ホスト型 見積・請求・入金管理／日本の適格請求書対応・マルチテナント前提）
**Branch state at end of day:** `origin/main` @ `089e8c3`（CI 全緑）
**Author:** Claude Opus 4.8（Invoice リナ）／指揮は統合リナ（施主指示）

---

## Summary

統合リナの指揮下 2 日目。着任時点で invoice は **仕掛かりゼロ・CI 全緑・v1.0.0 リリース済み**で安定。統合リナへ muster（現況＋待機3件）を投げたところ、**待機台帳の実測訂正**（②W2b 完結済み／③W3 は再生成方式で決着・順番待ち／①#425 は施主手番）を受領。そのうえで今夜の小粒 2 本を受けて完遂した。

**本日 main 入り 1本**（self-merge・統合リナ承認済み）:

| PR | Issue | 中身 |
| --- | --- | --- |
| **#698** | #697 | frontend-ci の paths フィルタ除去＋job `check`→`frontend-check` リネーム（required 昇格の前提整備） |

加えて **"integration" context の trigger 実測**（1行報告）を統合リナへ提出。

---

## Task 1: frontend-ci を required 昇格できる形に（#697 / PR #698）

### 罠の正体（実測）
- **同名 job による context 衝突**: frontend-ci の job 名が `check`、backend-ci の job も `check`。GitHub Actions のチェック context は **job 名そのもの**になるため、`required = ["check"]` がどちらを指すか曖昧になりうる。
- **paths フィルタ付き required の永久 BLOCK**: frontend-ci には `paths: frontend/**` 等のフィルタがあり、backend-only PR では job が **skip** される。required に入れると status が付かず、**mergeable にならず永久ブロック**（vault/contact が実証済みの罠）。

### 対応（第三の道）
1. push / pull_request 双方から `paths:` フィルタを除去 → **全 PR/push で常時実行**。
2. job `check` → `frontend-check` にリネームし context を分離。
3. backend-ci の `check` は**不変**・`e2e` job は現状維持（required 対象外）。

### 検証
PR #698 上で 4 context 全緑を実測:

| context | 由来 | 結果 |
| --- | --- | --- |
| `check` | backend-ci | pass 48s |
| `integration` | backend-ci | pass 39s |
| `frontend-check` | frontend-ci（リネーム反映） | pass 1m30s |
| `e2e` | frontend-ci | pass 2m19s |

- **リネーム反映を確認**: context が `frontend-check` として立った（backend `check` と分離）。
- **paths 除去の実証**: この PR 自体が `frontend/` 無変更（`.github/` のみ）にもかかわらず frontend-ci が走った＝skip ブロックの罠を回避できている。

merge SHA = `089e8c3`。統合リナが branch protection の required を **`[check, frontend-check, integration]`** に PATCH（integration も昇格採用・e2e は flakiness 観測枠で保留）。

---

## Task 2: "integration" context の trigger 実測（1行報告）

- 由来 = `backend-ci.yml` の `jobs.integration`（MySQL 8.4 ＋ PostgreSQL 16 で dialect-portable SQL を守る #396 のジョブ）。
- トリガー = `push`(main) ＋ `pull_request`(main)・**paths フィルタ無し** → **PR でも push でも走る**（PR #698 でも pass 実測）。required 昇格の「PR で走る」条件を満たすため、統合リナが決定的チェックとして採用。

---

## 副次アウトプット

- **記事ネタ供給**（フリート常設任務・施主 2026-07-18）: 「required × paths フィルタの永久 BLOCK 罠＋同名 job context 衝突」を articles リナへ relay（一次資料 = #697/#698/SHA `089e8c3`・再現 `gh pr checks <PR>`）。

---

## Next

- **待機**へ復帰。次は W1 の順番（payout→vault→origin→field→**invoice**）が nene2-js の tokens publish 後に回ってくる（統合リナ談）。
- #425 v1.0.0 の外向き公開実行は従来どおり**施主（税理士）確認→公開 GO**の手番。
- 統合リナから依頼・合図が届いたら対応 → 疑問は理由付きで統合リナへ → 施主判断分のみ上げる → 完了報告＋確認＋レビュー依頼。
