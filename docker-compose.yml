services:
  mailpit:
    image: axllent/mailpit:latest
    restart: unless-stopped
    ports:
      - "1585:1025"   # SMTP (NeNe Invoice 固定: 1585)
      - "8585:8025"   # Web UI — http://localhost:8585
    environment:
      MP_MAX_MESSAGES: 200
      MP_SMTP_AUTH_ALLOW_INSECURE: "true"
