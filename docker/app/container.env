# This file intentionally masks the host .env inside the container.
# All config comes from compose.yaml `environment:` so the Docker stack
# always uses MySQL regardless of the host .env (which targets SQLite).
